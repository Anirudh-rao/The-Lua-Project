function maximum(s)
    local max_index = 1  --intial case
    local m = s[max_index]
    for i = 2, #s do
        if a[i]
        then
           m = s[i]
           max_index = i
        end
    end
    return m,max_index --return max and its index
end